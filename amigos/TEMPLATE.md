# <Topic>

> Status: DRAFT - produced by the `amigos` skill. Sign-off blocked while any coverage area reads `TBD` (greenfield) or `THIN` / `MISSING` (adopted).

## Open Questions

Parked GAPs. Unresolved on purpose; do not block sign-off. Whoever consumes this spec next should read these first. Shape: `- [ ] <id or ASSUMED> — <question> — <why parked>`.

_None parked yet._

---

## 1. Topic / Goal

One sentence. What is being built or changed.

TBD

## 2. Problem Statement

Who hurts today, what the observable symptom is, and why now. Not the solution.

TBD

## 3. Scope of Work

What this change covers. Modules, collections, endpoints, contracts touched.

TBD

## 4. Out of Scope

Boundary defenses, not wishlist deferrals. Write what this change does **not** do and must not start doing during implementation.

TBD

## 5. Design / Mechanism

How it works. Data shapes, flow, storage, API surface, module boundaries. Enough that a reader can predict behaviour without reading the code.

TBD

## 6. Key Decisions

Each decision, the alternatives considered, and why this one. A decision that was never in doubt does not belong here.

| Decision | Alternatives | Why |
|---|---|---|
| TBD | | |

## 7. How We Test

Test strategy by layer - domain / use case / adapter / integration. What each layer proves and what it structurally cannot prove.

TBD

## 8. Edge Case Coverage

Enumerate the branches, including the negative and absent ones. For every optional field: present, absent, and empty. For every business flow: each condition, not just the happy path.

| Case | Expected | Covered by |
|---|---|---|
| TBD | | |

## 9. Rollout & Reversibility

Flag name, migration order, backfill, rollback path, and what to watch after deploy.

Omit this section only by replacing it with: `N/A because <reason>`.

TBD

---

## Change Log

Append-only. One row per substantive change - a decision, a scope boundary, a corrected premise, a superseded claim, a filled `TBD`, a reworded sentence. `Driven by` is exactly one of: an amigo finding id (`Dev G1-Q2`, `Ops F1-B1`), `user` (a decision the user made unprompted), or `draft` (the orchestrator's own synthesis - unreviewed, fair game at the next gate; may add, never supersede). A row that cites nothing means the change was nobody's - remove the change, not the row. `Why` reads as "otherwise <what breaks>".

| # | Section | Change | Driven by | Why |
|---|---|---|---|---|
| 1 | TBD | | | |
